# 猫车项目工作日志

用于记录猫咪追踪、网页控制、ROS2 导航及底盘联调的开发过程、验证结果和待办事项。

## 记录规范

1. **一项工作一条记录**：功能开发、问题修复、配置调整、测试联调和文档维护均可记录。完成阶段性工作后及时补充。
2. **统一编号与日期**：编号使用 `YYYYMMDD-NN`，每日从 `01` 递增；日期使用 `YYYY-MM-DD`，需要具体时间时使用 `HH:mm`，时区统一为北京时间（`Asia/Shanghai`，UTC+8）。补录历史工作只写已知日期，不推测时间。
3. **最新记录在前**：新日志放在「日志记录」下方最前面，保留既有编号。后续进展另建记录并引用原编号；修正旧记录时注明修订日期和原因。
4. **固定字段**：每条记录包含日期、记录人、类型、模块、状态、关联记录，以及下方模板中的五个内容小节。无内容时写「无」或「不适用」，不要留空。
5. **类型与状态统一**：类型使用「功能 / 修复 / 配置 / 测试 / 文档」之一；状态使用「进行中 / 待验证 / 已完成 / 阻塞」之一。核心验收尚未执行时标为「待验证」；阻塞时写明原因和解除条件。
6. **记录可核查的事实**：写清问题、改动及影响，文件使用相对链接；有提交或问题单时附实际编号，没有时写「未提交」或「无」。不要将计划写成已完成工作。
7. **验证结果可复现**：写明环境、执行目录、命令或操作步骤、预期与实际结果。区分静态检查、模拟测试和实车测试；未执行的项目明确标注「未执行」及原因。
8. **待办可执行**：使用 `- [ ]` 列出下一步及验收条件；完成项使用 `- [x]`。有已知限制时说明触发条件与影响。不记录 API Key、密码等凭据。

## 日志模板

复制以下模板到「日志记录」下方，替换所有占位内容。简单工作可每节只写一句，但保留字段与小节。

````markdown
### YYYYMMDD-NN｜工作标题

| 字段 | 内容 |
|------|------|
| 日期 | YYYY-MM-DD（北京时间） |
| 记录人 | 姓名或工具名称 |
| 类型 | 功能 / 修复 / 配置 / 测试 / 文档（选一） |
| 模块 | 涉及的模块 |
| 状态 | 进行中 / 待验证 / 已完成 / 阻塞（选一） |
| 关联记录 | 日志编号、提交、问题单；没有则注明无或未提交 |

#### 目标与背景

描述需求或问题现象，以及本次工作的目标。

#### 工作内容

- 具体改动及原因；相关文件：[文件名](相对路径)。

#### 验证与结果

- 环境与执行目录：填写实际环境和目录。
- 验证方法：填写命令或手动步骤。
- 预期结果：填写验收条件。
- 实际结果：通过 / 失败 / 未执行，并给出证据或原因。

#### 问题与限制

记录遗留问题、适用条件、阻塞原因；没有则写「无」。

#### 下一步

- [ ] 待办事项及完成标准；无待办则写「无」。
````

## 日志记录

### 20260911-01｜重整工作空间与网页 README 使用说明

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-11（北京时间） |
| 记录人 | Codex |
| 类型 | 文档 |
| 模块 | 工作空间使用说明、猫车网页文档 |
| 状态 | 已完成 |
| 关联记录 | 20260910-01、20260910-02；未提交 |

#### 目标与背景

将分散且部分过时的说明整理为可按顺序执行的使用手册，明确完整 ROS 启动与网页服务的关系。

#### 工作内容

- 重写 [工作空间 README](../README.md)，按日常启动、安装编译、硬件校准、建图保存、AMCL 导航、驾驶停止及故障排查组织内容。
- 重写 [网页 README](README.md)，提供双终端启动、视频识别、键盘驾驶、RViz 操作与服务退出步骤。
- 同步 [WEB_NAV_README.md](WEB_NAV_README.md) 和 [BROWSER_GUIDE.md](BROWSER_GUIDE.md) 的启动说明与文档入口。
- 根据启动源码纠正串口默认值、导航定位方式、单模块启动范围、实际 TF 链及 fake odometry 的限制；保留本机 `testroom` 地图示例。
- 说明当前避障轮廓含调试配置、视觉识别尚未接入自动跟随，以及保存地图脚本结果需核对等已知情况。

#### 验证与结果

- 环境与执行目录：本机 `~/nav_ws/src`，仅检查文档与源码。
- 验证方法：逐项对照 launch、YAML、URDF 与脚本；检查改动文档的本地链接、章节锚点及日志字段；对 README 和指南中的 Bash 代码块执行 `bash -n`；在根仓库与猫车仓库执行 `git diff --check`。
- 预期结果：命令语法有效，引用可定位，说明与当前源码一致。
- 实际结果：静态检查通过。未执行依赖安装、重新编译、ROS 启动或实车导航；文档检查不代表运行验收通过。

#### 问题与限制

现有代码的局部避障轮廓、fake odometry TF、终端遥控速度参数类型和速度仲裁等问题仅在文档中说明，本次未修改程序配置或实现。此前网页驾驶的实车验证仍待完成。

#### 下一步

- [ ] 校准实车避障轮廓后，按手册完成建图、保存、AMCL 初始定位与网页驾驶联调，并新增实测日志。

### 20260910-02｜建立项目工作日志与格式规范

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-10（北京时间） |
| 记录人 | Codex |
| 类型 | 文档 |
| 模块 | 项目文档 |
| 状态 | 已完成 |
| 关联记录 | 20260910-01；未提交 |

#### 目标与背景

建立统一工作日志，便于追踪开发改动、验证结果和后续联调事项。

#### 工作内容

- 新建 [WORK_LOG.md](WORK_LOG.md)，规定编号、日期、类型、状态、必填内容和验证记录方式。
- 提供可复制模板，补录网页键盘驾驶功能的实现与验证情况。
- 在项目 [README.md](README.md) 中增加工作日志入口。

#### 验证与结果

- 环境与执行目录：本地项目目录 `Cat-Tracking-RealSense/`。
- 验证方法：核对模板字段、日志顺序和相对链接，执行 `git diff --check -- README.md WORK_LOG.md` 检查已跟踪文档的差异格式，并单独检查新日志的行尾空白。
- 预期结果：两条日志字段完整，引用文件存在，无差异格式及行尾空白问题。
- 实际结果：通过；本次仅修改文档，不涉及程序运行或实车验证。

#### 问题与限制

无。

#### 下一步

- [ ] 后续每项阶段性工作按本文件模板补充日志，明确记录验证范围与遗留事项。

### 20260910-01｜网页新增 WASD 移动与 J/K 转向

| 字段 | 内容 |
|------|------|
| 日期 | 2026-09-10（北京时间，补录） |
| 记录人 | Codex |
| 类型 | 功能 |
| 模块 | 网页控制、ROS2 底盘通信 |
| 状态 | 待验证 |
| 关联记录 | 未提交 |

#### 目标与背景

在现有猫咪追踪与 RViz 网页中增加键盘驾驶，沿用项目已有麦轮遥控映射：W/S 前后移动、A/D 左右平移、J/K 左右转向。

#### 工作内容

- 在 [templates/index.html](templates/index.html) 和 [static/style.css](static/style.css) 中增加驾驶面板、速度滑块、连接状态、按键高亮及停止按钮。
- 新增 [static/teleop.js](static/teleop.js)，通过 rosbridge 的 9090 端口以 10 Hz 向 `/cmd_vel` 发布 `geometry_msgs/msg/Twist`。
- 默认移动速度为 0.20 m/s，转向速度为 0.50 rad/s；支持组合键、相反方向抵消及斜向合速度限制。
- 松键发送停止；空格、Esc、失焦及输入框交互关闭驾驶；断线清除按键状态，重连不自动恢复移动。
- 新增 [浏览器测试入口](tests/test_teleop_browser.py) 和 [模拟通信测试脚本](tests/teleop_browser.js)，更新 [WEB_NAV_README.md](WEB_NAV_README.md) 与 [BROWSER_GUIDE.md](BROWSER_GUIDE.md)。

#### 验证与结果

- 环境与执行目录：本机无头 Chrome，目录 `Cat-Tracking-RealSense/`；使用模拟 WebSocket，不连接实车。
- 验证命令：`python3 -m unittest discover -s tests -v`。
- 预期结果：六个方向映射正确，松键停止，组合键与速度调节有效，失焦、断线及重连不会遗留运动状态。
- 实际结果：1 个浏览器测试用例通过，包含 28 项场景检查；`git diff --check` 通过，并检查了桌面尺寸下的页面截图。
- 实车验证：未执行。本次仅验证浏览器行为及模拟消息，真实 rosbridge 链路与底盘运动效果仍需联调。

#### 问题与限制

- 当前没有速度指令仲裁。驾驶前需取消 RViz 导航任务，避免其他节点同时向 `/cmd_vel` 发布速度。
- 网络中断时停止消息可能无法送达；底盘驱动默认 0.5 秒速度指令超时机制仅在没有其他节点持续发送速度时生效。
- 现有启动脚本使用 HTTP/WS；HTTPS 页面需要另行配置 WSS。
- 点击 RViz 远程桌面后，按键由远程桌面处理；返回网页驾驶需重新启用。

#### 下一步

- [ ] 实车验证 W/S、A/D、J/K 的方向与速度符合面板设置。
- [ ] 实车验证松键、停止按钮、失焦及断线后的停车行为，确认重连不恢复移动。
- [ ] 完成以上验证后新增联调日志，引用本条编号并记录实测环境与结果。
