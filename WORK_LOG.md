# 上位机工作日志

### 20260917-01：网页双轴滑条控制与一键回零

- 读取同级 Cat_C 当前 USB 协议、故障位、默认零位保持及电机限位实现，以实际固件源码对齐上位机。
- 新增云台控制卡片：yaw/pitch 鼠标拖动滑条、目标及实测角度、启用、停止、一键双轴回零。Yaw 目标范围为 ±90°、Pitch 目标范围为 ±30°；步长 0.5°。
- 新增独立 Flask USB 控制服务，50 Hz 续发设定点，网页控制租约 600 ms；支持单会话控制、过期/乱序请求拒绝、失焦和离页停止、串口重连不恢复旧目标。
- 启用时保持实测角度；回零持续发送 0°/0° 位置目标，不修改编码器零位；显式停止发送锁存 STOP。被动查看页面不下发运动或 STOP。
- USB 客户端补全流式接收、CRC/版本检查、有限值与双轴范围检查、独占串口及写超时。串口通过 GIMBAL_USB_PORT 或 /dev/gimbal 配置，不自动猜测 ttyACM0。
- 按用户后续要求不修改 Cat_C；网页端 Yaw 滑条显示 ±90°、Pitch 滑条显示 ±30°，与当前下位机限位一致。
- 验证：12 项 Python 协议/服务测试通过；真实 Chrome 的 14 项云台交互检查和 29 项现有键盘驾驶检查通过。下位机代码未因本次网页功能修改。
- 限制：当前没有识别到已确认的 C 板 USB CDC，也没有 ARM 工具链，未进行完整 ARM 编译、烧录或实机转动。当前枚举的 WCH 串口未被当作云台打开。pitch 超过 ±30° 的指令需要烧录新固件后才能被旧下位机接受。

### 20260918-01：上位机 Pitch 限位与下位机对齐

- 按用户要求，上位机 USB 校验和网页 Pitch 滑条统一限制为 −30°～+30°；Yaw 保持 −90°～+90°。
- 未修改 Cat_C 下位机代码；一键回零和持续目标发送保持不变。

### 20260918-02：修复双 USB 设备串口编号变化导致的导航和网页连接故障

- 原导航默认连接 `/dev/ttyACM0`，接入云台后该编号属于 YueLuEmbedded Vision Comm port；底盘 WCH USB Single Serial 变为 `/dev/ttyACM1`。被动读取确认云台帧头为 AA55，底盘收到 30 个通过 BCC 校验的 24 字节反馈帧。
- 底盘完整启动、独立启动及参数文件默认端口统一为 `/dev/serial/by-id/usb-WCH.CN_USB_Single_Serial_0002-if00`，仍支持 serial_port 参数覆盖。避免将云台数据误当底盘里程计。
- 网页原默认 `/dev/gimbal` 在本机不存在；改用已验证的 `/dev/serial/by-id/usb-YueLuEmbedded_Vision_Comm_port_2070377C5948-if00`，保留 GIMBAL_USB_PORT 覆盖。
- 重启网页启动服务后，HTTP 云台状态 connected=true、controllable=true，实测 yaw 约 0.879°、pitch 约 -0.841°；fault=1 为上位机目标超时诊断，不是阻断电机故障。本次仅读取云台状态，未发送云台运动目标。
- 独立 ROS 域验证底盘收到 15 条 /odom 并发布 odom→base_footprint；启动底层 bringup 后收到 5 帧扫描，可在扫描时间戳查询 odom→laser_frame 以及 odom→base_link。测试节点已退出；未发送非零底盘速度或导航目标。
- 网页启动服务现运行于后台，日志为 `~/.local/state/cat-web/launch.log`；RViz 远程画面等待重新启动 RViz。Cat_C 代码未修改。

### 20260918-03：目标猫三维定位与 RViz 标注，叠加云台实测 yaw

- 网页参考猫匹配成功后，从对齐深度的框中心 5×5 邻域取有效深度中位数，按 RealSense 内参反投影三维光学坐标；只为目标猫发布位置。
- 新增 `cat_markers.py`：在推理前记录接收图像的 ROS 时间与云台反馈角度，以该时刻的 base_link→map TF 转换位置；发布 `/cat/position` 和 `/cat/markers`（绿色球体和坐标文字）。
- 用户指定相机近似位于车体中心、朝前、车顶高度，并进一步明确相机装在云台上，地图位置须叠加 yaw。最终 `camera_mount.json` 使用云台模式，相对 base_link 高 0.15 m（按当前 URDF 离地约 0.225 m），yaw=0 朝前、正角向左，转轴与相机偏移暂视为零。使用实测 yaw，不采用滑条目标；当前 follow_pitch=false，Pitch 按水平近似。
- 加入深度有效性、TF 可用性、云台反馈故障检查；目标丢失、停止追踪、读取失败时删除标记；标记在采集接收时刻起最长 2 秒内有效，过期结果不再发布。停止追踪后到达的旧推理结果不会恢复标记。
- 两份 RViz 配置增加 Target Cat 显示；网页追踪卡片显示标注状态和坐标。当前已经运行的网页需要重启加载新代码，已运行 RViz 需添加 /cat/markers MarkerArray 显示或重新加载配置。
- 验证：几何、有效深度、边缘取样、配置校验及停止竞态等 7 项测试通过；另 1 项真实 ROS 通信测试在隔离域 91 通过，验证实测 yaw 与 map 姿态组合、时间戳、Marker 类型/有效期、缺 TF 时不发布，以及停止/超时删除。此前整套 21 项测试通过，新增 yaw 配置场景后定向 8 项测试通过。
- 限制：实物相机高度、偏移、yaw 零位仍采用用户指定近似；当前不补偿 Pitch。采用主机接收图像时间，尚未硬件同步。没有自动移动底盘/云台，也未改下位机代码。

### 20260918-04：RViz 启动自动加载目标猫标注

- 确认建图和导航 RViz 配置均包含并启用 Target Cat（MarkerArray，/cat/markers），并默认展开该显示项。
- 核对 slam_navigation.launch.py 加载 navigation.rviz，bringup 的建图/导航模式分别加载 slam.rviz/navigation.rviz。
- 实际 install 目录为源码符号链接，两份安装配置已校验，无需重新编译。重新启动或加载 RViz 配置即可自动订阅，无需手动 Add。

### 20260918-05：整理最终交付日志并提交网页与导航配置

#### 最终行为

- 网页云台支持 Yaw ±90°、Pitch ±30°滑条控制、一键回零、实测反馈及失联停止；默认使用已确认云台的稳定 USB 设备路径。
- 识别到目标猫后发布地图坐标和 RViz 标记，叠加采集时刻的云台实测 yaw 与车体 TF；目标丢失、深度无效或数据过期时清除标记。
- 相机近似位于车体中心、车顶高度，相对 base_link 高 0.15 m；yaw=0 朝前、正向向左。当前不叠加 Pitch，安装偏移仍待实物标定。
- 建图、导航 RViz 启动自动加载并启用 /cat/markers；底盘默认端口使用稳定 WCH 路径，解决与云台 USB 枚举顺序变化造成的 odom 缺失。
- 修正文档残留的 Pitch ±90°及烧录要求，移除依赖已撤回下位机测试改动的 GCC 命令。Cat_C 保持原代码。

#### 验证

- 在 ROS Humble 环境执行 `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v`：22 项测试全部通过，包括真实 Chrome 的云台/键盘交互及隔离 ROS 域中的坐标、TF、标记生命周期检查。
- 本会话此前已验证实际底盘串口反馈、15 条 /odom、扫描时间戳上的 odom→laser_frame TF，以及网页云台状态 connected=true。
- `git diff --check` 通过；网页代码测试不代表完成实机猫位置精度标定。

#### 提交范围

- 网页仓库提交控制、标注、相机配置、测试、说明与本日志；工作空间提交底盘稳定串口配置、RViz 默认显示、说明及网页仓库版本引用。
- Python 运行缓存不纳入提交；未修改下位机源码，未烧录或推送远端。

### 20260921-01：自动跟随目标猫，1 米停车，第二轴按 Roll 处理

- 接续 2026-09-18 的开发与用户反馈；以最后要求为准，停车目标为 base_link 中心到猫标注点水平距离 1 米。新增「自动追踪 / 停止自动追踪」，视觉识别启动不会直接动车。
- `auto_follow.py` 使用已发布且采集时间不超过 1 秒的 map 目标与当前车辆 TF，计算距猫 1 米且朝向猫的 Nav2 目标。每秒最多重规划一次，目标移动阈值 0.25 m；旧目标取消完成才发送新目标。1.1 m 内停止接近，超过 1.3 m 恢复，避免距离抖动，不主动倒退。
- `follow_navigation.py` 接入 NavigateToPose，跟随期间以 SpeedLimit 限制 Nav2 平移为 0.20 m/s，取消期间保持限速。处理延迟接收、拒绝、失败、取消 ACK 与终态的区别；停止后迟到接受仍会取消，迟到拒绝释放取消状态。
- 按用户确认，USB pitch 字段实际对应 Roll。页面显示改为横滚 Roll，手动范围 ±30°，Yaw ±90°；标注配置 second_axis=roll、follow_secondary=true。自动跟随用 Yaw 水平瞄准，Roll 逐步归零，角度变化限制 40°/s。无物理俯仰轴，无法主动上下居中；安装高度、符号与零位仍需实物标定。
- 自动会话网页心跳 200 ms、租约 800 ms；目标丢失/过期、定位丢失、云台故障、导航失败、页面隐藏/失焦/离开及显式停止均结束跟随，不自动恢复。相机/识别停止和目标切换也取消自动任务。
- 同页面手动云台交接等待释放完成；开始前拒绝其他 Nav2 任务。网页键盘改发 /cmd_vel_manual，由后端在非跟随、非取消、无其他 Nav2 任务时转发至 /cmd_vel，并提供 350 ms 手动超时。其他直接 /cmd_vel 发布者不受该接口仲裁。
- 更新 README、WEB_NAV_README、相机配置示例及标注坐标文档。需要重启 Python 网页服务并 Ctrl+F5；ROS 环境必须先加载。
- 验证：34 项测试通过（见本次最终复核），包括 10 项跟随策略测试、实际 Chrome 的 10 项自动跟随交互检查及既有云台/键盘检查、隔离 ROS 域 92 的模拟 Nav2 action 与手动转发测试、域 91 的标注测试。未向真实底盘发送非零速度或导航目标，未进行实车自动跟随。

### 20260921-02：左侧半米处闪烁障碍的采样诊断与配置修正

- 用户确认闪烁离车身约半米。2026-09-18 只读检查现场参数，局部 footprint 确实是 1 cm 见方；恢复为已有全局的 0.80×0.70 m，并明确 footprint_clearing_enabled=true。此项不能单独解释车体外的闪烁。
- 读取 /scan、/livox/lidar 与 TF，确认 laser_frame 到 base_link 位移 (-0.20,0,0.225)，Z 向下。主要近水平平面拟合 z≈0.269+0.0356*x−0.0048*y m，3997/5000 点入选，实际地面候选位置与原 0.30 m 假定不符。
- 二维扫描 max_height 从 0.20 调到 0.17。50 帧同一批点云的阈值对比：指定左侧区域候选点 36→2，全部候选点 205694→191108。此结果只证明过滤效果，不证明所有被删点都是噪声或问题已彻底解决；不同窗口曾出现更多左侧点，仍需启动后现场复测。
- 未改变原始点云、雷达通信、TF、距离盲区或屏蔽左侧扇区；未将 MID360 未采样角度的 inf 无条件作为清除射线。固定高度筛选会减少约 7～10 cm 的真实低障碍检测，限制与恢复排查方法写入工作空间 README。
- YAML/JSON/Python AST 与 git diff --check 通过；本机 install 的两份配置均链接源码，重新启动包含雷达的完整导航 launch 即生效，无需编译。2026-09-21 收尾时未发现导航或网页服务运行，因此未启动任何实车运动服务。Cat_C 下位机未修改；本次改动尚未提交。

### 20260921-03：键盘启用自动取消导航，提高速度上限

- 用户反馈导航卡在目标朝向时，键盘控制无效。原因是已有后端在其他 Nav2 任务仍活跃时拒绝转发手动速度，网页也会在自动跟随期间锁定启用按钮。
- 新增 POST `/api/teleop/enable`：停止自动跟随，向 `/navigate_to_pose`、`/navigate_through_poses`、`/follow_waypoints` 的 CancelGoal 服务请求取消当前任务，包括其他客户端/RViz 发起的任务；等待任务终态及当前 1 秒导航速度平滑器尾部消退后，再允许浏览器启用键盘。不依赖 map TF，因此定位无效时仍可请求接管。
- 切换期间拒绝手动速度，导航取消失败或 6 秒内未确认停止时返回错误，键盘保持关闭。兼容 Humble 对 cancel-all 被拒绝时可能返回成功但取消列表为空的情况，继续依据任务状态阻止接管。
- 浏览器增加等待、错误提示及异步请求过期保护；导航期间可点击启用，失焦、离页、停止、空格/Esc 会使迟到的接管响应失效。同页面自动跟随先结束心跳，接管完成后清除旧状态，防止界面误锁。
- 平移速度滑块上限 0.50→1.00 m/s，转向上限 1.00→2.00 rad/s；默认仍为 0.20 m/s、0.50 rad/s。核对现有底盘驱动上限为 1.00 m/s、3.14 rad/s，无需修改底盘或下位机。
- 验证：ROS Humble 环境下 35 项测试全部通过；Chrome 验证导航期间可接管、等待期间不移动、失败/失焦后不启用以及最大档速度发送。隔离域 93 中同时启动三个外部 Nav2 模拟任务，验证拒绝取消时不接管、取消达到终态后手动速度可转发、无 map TF 仍可接管；未向实车发送测试运动指令。Python 语法与 git diff --check 通过。
- 文档与页面提示已同步。重启网页 Python 服务并 Ctrl+F5 后生效；导航参数和下位机不需要为此调整。其他直接发布 /cmd_vel 的程序不受本功能取消。

### 20260921-04：提交前审计与竞态修复

- 审计当前全部未提交改动：自动跟随、Roll 坐标补偿、导航取消与键盘接管、速度上限、网页生命周期、点云投影与代价地图配置，并核对两层 Git 仓库提交范围。
- 修复接管期间长期持有 AutoFollow 锁的问题：以短锁保留切换状态，等待 ROS 取消时释放锁；状态查询与停止接口保持响应，重复接管和自动跟随启用立即被拒绝，不会排队后意外执行。
- 修复旧状态 HTTP 请求迟到失败会停止新自动跟随会话的问题；错误响应与成功响应均按 revision 排除旧请求。
- 修复旧导航取消 ACK 在任务终态/新取消请求之后到达时覆盖当前 cancel_future 的问题，仅处理仍属于当前取消请求的回执。
- 新增接管并发/失败恢复、旧回执顺序、旧状态失败的回归检查，以及同时加载 teleop/gimbal/auto-follow 三份真实脚本的 Chrome 切换测试。修正文档残留的手动取消导航要求。
- 验证：38 项完整 Python 测试通过（含隔离 ROS 域 91/92/93 与 Chrome）；随后新增组合切换测试，4 项浏览器测试全部通过，最终共覆盖 39 项测试。Python AST、JSON/YAML、全局/局部 footprint 一致性及两仓库 git diff --check 通过。
- 仍需实机验证自动跟随精度、运动中障碍闪烁和近地小障碍检测；max_height=0.17 会降低约 7～10 cm 低障碍检测能力，此项限制已在工作空间 README 明确记录。没有向实车发送测试运动指令。
- 本次按用户要求提交：网页仓库包含代码、配置、测试与日志；工作空间包含雷达/导航配置、说明及网页 gitlink 更新。排除全部 Python 缓存，Cat_C 下位机工作树未修改；只提交本地，不推送远端。

### 20260921-05：网页人物测试模式与参考衣着识别

- 新增「切换到人物测试模式 / 退出测试模式（识别猫）」按钮，同步更新模式状态、参考照片提示和视频状态。默认猫模式，人物模式使用当前衣着的单人参考照；切换不会自动启动车辆。
- 新增 target_tracking.py：沿用现有 YOLOv8n 与 ResNet50，猫模式保留 class=15、0.75 阈值和 0.05 分差；人物模式只检测 class=0，参考图必须恰有一个有效人体。裁掉头部、脚部及边缘背景，组合 ResNet 衣着外观（0.45 权重）与上下身 HSV 分布（0.55 权重）；综合阈值 0.78、分差至少 0.08、两项各至少 0.65，连续 3 次空间相邻检测后确认。歧义、低分或丢失时不标注目标。
- 新增 TrackingSession 管理模式代次、参考照片、模型初始化和推理结果：切换先清除标记和停止自动跟随，停止/切换后迟到初始化不能恢复旧目标，旧帧不能标注新模式。上传文件使用唯一文件名并绑定模式代次，禁止复用切换前的参考路径；初始化错误直接返回页面。
- 前端处理模式切换、上传、模型初始化、其他页面状态更新的迟到响应；切换需重新上传对应参考照片。修复拖拽文件仅显示预览而未作为上传文件的问题。
- 人物目标沿用 /cat/position 和 /cat/markers 及现有 RViz 配置，显示文字改为 Target person。自动追踪继续按车体中心到目标约 1 米停车，Yaw 朝向目标、Roll 保持水平；切换、识别停止、目标丢失等仍触发停止。
- 测试：48 项完整测试通过；随后加入猫模式检测类别与原阈值回归（7 项外观/会话测试通过），并扩展隔离 ROS 标注测试验证 Target person 文本与旧采集失效（1 项通过），最终覆盖 49 项。包含 Chrome 人物模式的 6 项操作检查、Flask 接口、异步初始化、匹配歧义与原控制回归；AST 与 git diff --check 通过。
- 真实模型烟雾检查：使用 Ultralytics 公共 bus.jpg（https://raw.githubusercontent.com/ultralytics/assets/main/im/bus.jpg），从同图裁出单人参考，在含 3 个有效人体检测的原图中匹配分约 0.948，另两人未通过单项门槛；前两次未确认，第 3 次确认目标。临时图片仅放 /tmp，不纳入仓库；此检查不代表跨视角、换衣或遮挡鲁棒性验证。
- 限制：当前是衣着外观匹配的测试方案，未使用专门人物 ReID 模型，不确认姓名或真实身份；同款衣着、换衣、背身和遮挡可能误匹配或漏检。没有实车自动跟人测试，未修改下位机，也未向真实车辆发送测试运动。重启网页服务并 Ctrl+F5 生效；本次功能尚未提交。

### 20260921-06：导航与键盘响应诊断、加减速与末端朝向调整

- 用户反馈移动和转向加速度低、延迟大、无法到达目标朝向。当前无运行中的 ROS 节点，先审查控制链并使用隔离测试，未启动真实底盘运动。
- 导航 DWB/平滑器平移加速度 2.5→4.0 m/s²，角加速度 3.2→6.4 rad/s²；制动绝对值分别 6.0 m/s²、10.0 rad/s²。平滑器 20→50 Hz、断流超时 1.0→0.25 s。ROS 速度上限对齐为平移 1.0 m/s、转向 2.0 rad/s，关闭轨迹详细调试输出。
- 修正末端评分：RotateToGoal.scale 0.5→16、Twirling.scale 10→0.1，避免转向惩罚压过目标朝向收益；trans_stopped_velocity 0.25→0.05，yaw_goal_tolerance 0.25→0.10 rad。低速进度检查改为 15 秒/0.1 m，Spin 恢复行为最高角速度 0.1→0.8 rad/s，不能与末端 DWB 控制混淆。
- 键盘速度不经过 Nav2 平滑器，原驱动没有加速度斜坡；键盘续发 10→20 Hz，后端速度转发/接收与底盘订阅改为只保留最新一条。保留松键立即零指令、失联看门狗与键盘接管的取消等待。
- 发现关键限制：串口编码 round(vx*3.5)、round(vy*3.5)、round(wz*10) 会将低于约 0.143 m/s 的单轴移动及 0.05 rad/s 的转向归零；0.2 m/s 的 45° 斜向也可能归零。历史 cef6fb3 明确记录实车平移倍率标定，1269599 调整转向倍率，故没有按通用文档盲目改成 ×1000。用户已确认仍为同一套底盘和固件，因此保留原标定倍率；小速度量化限制仍需通过实测和控制/固件方案另行处理，不宣称已彻底解决实车低速定位。
- 新增只读 record_velocity_response.py 记录手动/Nav2/最终速度和里程计，便于区别上位机延迟与底盘量化、标定或速度环问题。工作空间 README 记录参数表、限制与使用方法。
- 验证：底盘驱动编译成功；隔离域 95 内真实 Nav2 平滑器与伪串口驱动 2 项测试通过，阶跃全幅约 0.247 s、制动约 0.157 s、断流归零约 0.381 s；伪串口指令响应约 0.021 s，并复现小指令归零。网页全套 49 项测试通过。未改下位机代码；尚未实车验证加速度、制动与到达姿态。

### 20260921-07：人物相似度显示为零的诊断与修正

- 用户反馈上传本人照片后，本人和其他人的相似度都显示为 0。代码确认人物匹配将任一单项低于 0.65 的综合分强制清零，掩盖了真实分数；没有现场参考图与视频，不能判断实际是外观还是颜色门槛未通过。
- 保留真实加权综合分，单独记录外观分、颜色分与单项筛选结果；视频框显示 Appearance、Color 和 PASS/LOW。单项不合格仍不能成为目标，综合阈值与连续三次确认不变；竞争者分差按真实分数计算，避免清零掩盖歧义。
- 文档补充当前衣着、现场光照及相近取景要求；该模式使用衣着匹配，头像照和换衣照片不适合作为参考。没有盲目降低阈值，也没有宣称解决跨视角人物重识别。
- 验证：人物匹配/会话 9 项测试及 Flask/视频帧 3 项测试全部通过，覆盖不合格高综合分不得跟随、不同衣服仍显示非零外观贡献、竞争歧义、连续确认及实际视频文字渲染。未访问相机或发送运动指令。重启网页服务并刷新页面生效；尚未提交。

### 20260921-08：人物综合判断阈值调整

- 按用户要求将人物综合相似度阈值从 0.78 改为 0.80；猫模式维持 0.75。外观/颜色单项门槛 0.65、人物竞争分差 0.08 与连续三次确认条件保持不变。
- Appearance 是包含纹理、形状和颜色信息的 ResNet 外观特征；Color 是上下身 HSV 颜色分布相似度。两项存在部分重叠，当前综合分仍按 45%/55% 加权。两份使用说明同步更新；重启网页服务生效。
- 验证：既有人物/会话 9 项测试通过；额外边界检查确认人物 0.79 拒绝、0.80 连续确认后通过，猫 0.75 通过；git diff --check 通过。

### 20260921-09：降低人物颜色权重

- 按用户要求，人物综合分改为 Appearance × 90% + Color × 10%（原为 45%/55%）。人物综合阈值仍为 0.80，外观和颜色单项最低分仍各为 0.65，分差与连续确认不变；猫模式不变。
- 同步使用说明和既有测试期望；验证人物匹配/会话 9 项及网页/视频 3 项测试通过，包含综合分超过阈值但颜色不合格时仍不选为目标；git diff --check 通过。重启网页服务生效。

### 20260921-10：人物识别替换为专用 OmniScaleNet ReID

- 用户要求使用专门人物模型。新增 person_reid.py，采用 Open Model Zoo person-reidentification-retail-0287（OmniScaleNet + LCT，256 维特征），通过 OpenVINO 2024.6.0 在 CPU 推理。人物不再初始化猫的通用 ResNet50；猫模型与 0.75 阈值保持不变。
- 参考与实时人物均使用完整检测框，按官方 BGR、0–255、NCHW 1×3×256×128 输入，模型 IR 已含归一化。输出须为有限非零 256 维向量，归一化后使用余弦相似度；异常会重置连续确认计数并由现有流程清除目标。
- 人物综合分保留 ReID 90% + Color 10%，综合阈值 0.80、竞争分差 0.08、连续三次空间相邻确认。取消 Color 单项 0.65 硬门槛，避免颜色辅助项继续单独否决人物；保留 ReID 至少 0.65 和数值有效性检查。视频原 Appearance 改标为 ReID，网页模式提示同步更新。
- 模型固定使用官方 2023.0 FP32 下载源；XML 451147 字节、BIN 2362128 字节，按官方 SHA-384 校验，临时下载完成且通过校验才替换缓存。默认缓存 ~/.cache/cat-tracking/person-reidentification-retail-0287，可离线复用；加载失败明确报错，不回退通用模型。模型来源、许可和部署说明见 docs/person_reid.md。
- 本机已安装运行时并下载/校验模型；安装依赖将 numpy 从 2.2.6 调整为 OpenVINO 支持的 2.1.3，现有 OpenCV/PyTorch 导入与完整测试通过。运行 python3 person_reid.py 已完成真实 CPU 推理自检。
- 真实模型试跑使用此前 /tmp 中的公开 Ultralytics bus.jpg 及同图裁剪参考：目标 ReID 约 0.955、综合约 0.954；另两人 ReID 约 0.202/0.228、综合约 0.228/0.262；前两次不确认，第 3 次确认。参考图亮度缩减及镜像 ReID 分约 1.000/0.985；这只是同图及合成扰动验证，不代表跨相机/角度实测，也不是用户本人识别准确率。
- 验证：58 项完整测试全部通过，含新增模型缓存校验/损坏恢复/离线失败、BGR 预处理、非法特征拒绝、人物不加载猫模型、取消颜色硬门槛、推理异常重置确认、0.80 阈值边界，以及原 Chrome/Flask/隔离 ROS 回归。git diff --check 通过。未修改下位机，未启动实车运动，未提交；重启网页服务并刷新后，重新上传当前全身单人照验证现场效果。

### 20260921-11：人物综合识别阈值改为 0.25

- 按用户要求将人物综合阈值从 0.80 改为 0.25，移除 ReID 单项 0.65 门槛，避免综合阈值调整后仍被旧门槛阻挡。人物评分权重仍为 ReID 90% / Color 10%；猫阈值仍为 0.75，人物竞争分差 0.08 与连续三次确认条件保留。
- 视频 PASS/LOW 现在反映数值有效且综合分是否达到 0.25；PASS 仍不等于完成分差和连续确认。同步三份使用/模型说明。
- 验证：人物匹配/会话 11 项、网页/视频 3 项测试通过，包含 0.249 拒绝、0.25 通过，以及 ReID 0.2、Color 1.0 得到综合 0.28 后可连续确认，不再受旧单项门槛阻挡。git diff --check 通过；重启网页服务生效，尚未实车验证或提交。

### 20260921-12：自动跟随容忍短暂识别闪烁

- 用户反馈识别闪烁导致自动追踪无法正常工作。确认原流程中单次目标未确认就会清空标注，AutoFollow 将无目标直接当作终止错误；人物重新识别又需三次连续确认，无法在原会话中恢复。
- 区分临时无观测与定位/设备/导航故障。以最后确认目标的原始采集时刻计时：0.5 秒内保留已有导航任务但不新发目标，之后请求取消导航并保留会话等待；3 秒内新鲜且确认的观测可恢复，已取消的导航须等到终态才允许新目标。超过 3 秒彻底结束，迟到的新目标不能恢复会话。
- 目标新鲜度从 1.0 秒收紧到 0.5 秒，与短时运动缓冲一致；未收到新图像时也不能继续用旧位置追赶。重复旧观测和网页心跳不会延长目标丢失超时；等待时距离显示清空，云台续租并保持最后指令角度。
- 等待期间仍检查网页 0.8 秒租约、地图定位、外部导航、导航失败及云台故障。手动停止、键盘接管、停止识别或切换模式沿用立即结束流程。人物 0.25/猫 0.75 阈值、人物连续三次确认与 RViz 清除过期位置规则不变。
- 页面提示、README、WEB_NAV_README 和 ReID 说明同步更新。页面通过 active=true 保留原会话心跳，显示“等待重新确认”，不自动发起新会话。
- 验证：64 项完整测试通过，包含反复人物确认闪烁、短时任务保持、较长丢失取消与恢复、等待导航终态、旧帧/超时不可恢复、等待期间故障/手动停止、真实 Chrome 会话续租与提示，以及原隔离 ROS 回归。git diff --check 通过；没有启动真实车辆测试运动。重启网页服务并刷新生效，本次尚未提交。

### 20260921-13：人物识别阈值改为 0.20

- 按用户要求将人物综合阈值从 0.25 改为 0.20；ReID 90% / Color 10%、分差 0.08、连续三次确认与自动追踪丢失缓冲均保持现有设置，猫阈值仍为 0.75。同步使用说明。
- 验证：人物匹配与会话 11 项测试通过，包括 0.199 拒绝、0.20 连续确认后通过以及猫模式回归；git diff --check 通过。重启网页服务生效，尚未提交。

### 20260921-14：提高自动瞄准响应、减少底盘多余转向

- 用户反馈云台转动慢、底盘行进时多余转向多。检查确认自动跟随对两轴统一限速 40°/s，且每次跟随目标朝向均指向目标人物/猫；目标横移时会同时要求车头与云台转向。手动滑条本来直接发送角度，USB 定时发送为 50 Hz。
- 自动 Yaw 指令变化速度提高到 120°/s，Roll 保持 40°/s，保留 ±90°/±30°范围、网页租约及故障停止。这里只提高上位机目标变化速度，下位机电机环、输出限制和实际机械响应未修改也未实测。
- 自动跟随记录启用时的地图车体朝向，后续导航目标均使用该朝向；全向底盘平移接近，云台负责瞄准，避免每次重规划都要求车头朝向目标。等待目标恢复保留原朝向，结束后清空，新会话采用新的当前朝向。目标超出云台范围仍停止，不自动转车搜索；Nav2 避障/恢复仍可转向。
- Nav2 Twirling.scale 从 0.1 适度提高至 1.0，抑制行进时的额外旋转；保留 RotateToGoal.scale=16、0.10 rad 末端朝向容差以及既有加速度，普通 RViz 导航仍使用用户指定的目标朝向。全局评分调整仍需实车确认，不声称消除所有转向。
- 验证：66 项完整网页/策略/Chrome/隔离 ROS 测试通过，新增验证 Yaw 首 50 ms 前进 6°、Roll 2°且不超调，目标横移与车体偏航不改变会话目标朝向，重新启用采用新朝向。YAML 及安装符号链接检查、两仓库 git diff --check 通过。Cat_C 未修改，未向实车发送测试运动。重启完整导航与网页服务并刷新生效，无需编译；尚未提交。

### 20260922-01：专用麦轮平移跟随，区分多余转向与串口低速归零

- 用户反馈目标追踪卡住且不断转向，要求继续处理。先前固定导航目标 yaw 和提高 Twirling 评分并不禁止途中旋转。只读查询当时运行参数确认仍仅有 FollowPath，max_vel_y=1、max_vel_theta=2、Twirling=1，默认行为树含 Spin(1.57 rad)。因此不能将此前修改解释为只平移。
- 当时只读采集 12 秒收到 cmd_vel_nav 149 条、cmd_vel 386 条、odom 240 条；后续会话中临时 CSV 已不可用且服务退出，未据此宣称测得实际卡顿根因。历史控制器日志有频繁取消/重发任务及取消结果等待错误，识别重获/目标更新也可能影响连续行进。没有主动给真实底盘发送测试速度或目标。
- 新增 FollowTarget 控制器，x/y 支持正负平移、合速度上限 0.20 m/s，角速度限制为 0；不加载 RotateToGoal/路径朝向评分，使用 ObstacleFootprint、Oscillation、PathDist、GoalDist。专用 target_goal_checker 忽略 yaw，位置容差 0.08 m，避免超过跟随 0.10 m 停车缓冲后反复成功重发。
- 网页 NavigateToPose 显式携带安装目录 config/follow_target.xml，树只规划和跟随，失败直接停止，不执行 Spin、BackUp 或清空代价地图恢复。仍保留障碍规划和完整轮廓碰撞检测；无法固定朝向平移通过的狭窄处会失败，不承诺直线穿障碍。
- 新增第二个 goal checker 后，复制 Nav2 Humble 普通单点/多点恢复树，仅明确 general_goal_checker；两份导航 launch 指向本包安装配置，保留普通导航的朝向与恢复功能。自定义行为树也需显式指定 goal_checker_id，不能依赖单检查器默认选择。
- 关键限制仍未消除：0.20 m/s 的 45°斜向分量约 0.141421，按既有 round(v*3.5) 编码均为 0。伪串口真实驱动再次复现；没有盲目提高速度、强制最小输出或改成 ×1000。底盘与固件已由用户确认未变，保留标定倍率；需实测最小可控速度后决定控制/串口方案，不能宣称卡住已完全修好。
- 验证：网页 66 项完整测试通过；隔离域 97 真实 Nav2 控制器/代价地图/导航行为树 3 项测试通过，覆盖横移、斜移输出 yaw=0、目标 yaw 不同仍成功、普通导航末端仍输出转向、障碍阻挡失败，以及真实 bt_navigator→模拟规划器→真实 FollowTarget 全链路。原隔离平滑器/伪串口 2 项也通过，并新增斜向量化归零检查。累计覆盖 71 项；YAML/XML/launch 语法、安装 XML 与源码一致性、两仓库 git diff --check 通过。
- wheeltec_bringup 已编译安装，必须重启完整导航和网页以加载第二套控制器与新行为树，不能只刷新页面。两层 README 与页面提示已更新；Cat_C 未修改，尚未提交。本次完成旋转策略修正，实车最小速度、低速跟随及避障效果仍待验证。

### 20260922-02：人物识别阈值改为 0.10

- 按用户要求将人物综合识别阈值从 0.20 改为 0.10。ReID 90% / Color 10%、竞争分差 0.08、连续三次确认和自动跟随逻辑保持原设置，猫阈值仍为 0.75；同步两份使用说明与模型说明，未改动运动速度参数。
- 验证：人物匹配/会话 11 项及网页/视频 3 项测试通过，覆盖 0.099 拒绝、0.10 连续确认后通过、低分显示及猫模式回归；git diff --check 通过。重启网页服务生效，尚未提交。

### 20260922-03：自动追踪持续等待，手动停止结束

- 按用户要求取消目标丢失 3 秒后结束会话的限制。目标丢失时仍保留最后采集后 0.5 秒的短暂保持，随后请求取消导航、清空目标距离并持续等待，没有等待时长上限；云台保持最后指令角度并续租，不追赶过期位置。
- 支持按下自动追踪时尚未确认目标：导航定位、云台与控制权可用即可进入等待，新鲜且确认的目标出现后开始跟随。目标暂时超出云台范围时也改为立即暂停导航并持续等待，不自动转车或云台扫视搜索。
- 重新获得目标后复用原会话，等待旧导航任务取消达到终态后继续；旧采集帧不能恢复导航。手动停止后不会因新识别结果重新启用。人物连续确认、匹配阈值、1 米停车距离与只平移控制保持现状。
- 网页心跳租约、失焦/离页、手动接管、停止识别/相机或切换模式、定位/导航/云台故障仍按原流程结束，不将设备故障视作目标丢失。已在页面与说明中明确，持续等待需保持页面连接。
- 验证：66 项完整测试通过，包含模拟丢失 60 秒不退出、无目标启用并等待、旧帧不恢复/新帧恢复、等待旧导航终态、目标超出范围等待、手动停止不能重启；Chrome 连续 200 次状态/心跳检查确认等待中保留会话及停止按钮，既有设备/通信故障与隔离 ROS 回归通过。两仓库 git diff --check 通过；未向实车发送测试运动。重启网页服务并刷新生效，本次尚未提交。

### 20260922-04：车体对准目标，使云台 Yaw 尽量回中

- 按用户新要求，在既有平移跟随基础上增加主动车体纠偏，而非始终禁止转向。新鲜目标所需的云台 Yaw 偏角达到 20° 时进入纠偏，降到 ±8° 内退出；中间区间保留当前阶段，避免小幅摆动反复切换。云台持续先行瞄准，不直接强制归零而丢失目标。
- 车体目标角度按当前车体 yaw + 目标所需云台 yaw×yaw_sign 换算，使用短角差处理 ±π 环绕；目标位置沿用实测云台反馈参与的地图定位。当前位置即为纠偏目标 x/y，接近 1 米停车区仍允许纠偏；只在位置变化 0.25 m 或纠偏朝向变化 10° 以上重规划，最多每秒一次。
- 新增 AlignTarget DWB 和 align_target.xml：x/y 速度均为 0，最大角速度 0.4 rad/s（约 23°/s），角加/减速度参数 1.2/-2.0，ObstacleFootprint 检查旋转轮廓，RotateToGoal 纠正目标朝向，使用 general_goal_checker 的 0.10 rad 容差。Navfn 显式 use_final_approach_orientation=false 以保留原地纠偏目标 yaw。该行为树不含 Spin、BackUp 或清图恢复。
- 平移和纠偏任务切换先取消旧任务，等终态才发送新任务；丢失目标仍在 0.5 秒缓冲后暂停并持续等待，超出云台范围不盲转搜索，设备/定位/导航失败仍结束。新增 centering 状态及页面说明；重启完整导航和网页加载新控制器。
- 验证：68 项网页完整测试通过，之后补充平移→纠偏等待取消的测试，23 项跟随策略测试全部通过（最终网页覆盖 69 项）。隔离域 97 中真实 Nav2 的 5 项测试通过，包含双向低速纠偏无平移、单点旋转路径、旋转碰撞拒绝、普通导航/平移回归，以及真实 bt_navigator 在平移树与纠偏树间切换并成功到达。策略还覆盖 20°/8°滞回、1 米内纠偏、yaw_sign、角度环绕、丢失目标暂停；ROS action 回归验证纠偏树路径正确传递。两仓库 git diff --check、安装 XML/参数检查通过。
- wheeltec_bringup 已编译安装，未向真实底盘发送测试运动，未修改 Cat_C。实际转速、云台零点与最小角速度量化仍需实测，不保证机械 Yaw 严格等于 0°；本次尚未提交。

### 20260922-05：撤销相机分辨率调整

- 按用户要求撤销本次高清相机修改：彩色和深度恢复 640×480、30 FPS 采集配置，JPEG 质量恢复 85，帧生成节奏与显示恢复原实现；移除高清档位、环境变量、画面质量状态及对应测试和文档。
- 保留此前人物识别阈值 0.10、持续自动追踪、车体纠偏使 Yaw 回中等修改，未改动下位机或正在运行的相机进程。
- 验证：69 项完整网页、策略、浏览器和隔离 ROS 测试通过，git diff --check 通过；尚未提交。

### 20260922-06：修复前方地面假障碍，人物综合阈值改为 0.15

- 用户报告前方约两米和左侧半米障碍点，随后明确仅处理前方、忽略左侧。只读采集运行中的 `/livox/lidar`、`/scan`、TF、里程计和代价地图；两段各 12 秒，车辆里程计位置及速度均未变化，没有发出测试运动。
- 第一段 120 帧原始点云/119 帧扫描中，车体前方 `1.3<x<2.7, |y|<0.5` 米有 3537 个二维候选点，全部距拟合地面不足 2 厘米。地面 `laser_z≈0.26925+0.04307*x−0.00399*y`；倒装雷达的负 X 对应车体前方，倾斜地面在约两米处越过旧固定 max_height=0.17 门槛。原始点云已存在该几何投影问题，不能只清代价地图。
- 新增 wheeltec_bringup/scripts/ground_filter.py 和参数文件，每帧拟合并验证标定高度/斜率容差、最少 300 点、60% 支持比例及多方向空间覆盖。只删除距确认平面 3cm 内、雷达水平距离 4m 内的点；拟合不足/异常原样透传。完整 PointCloud2 字段、字节记录和采集时间保留，原始点云不变，不增加等待确认帧数。
- 导航启动默认接入 `/livox/lidar → /livox/lidar_obstacles → /scan`，`filter_ground:=false` 可回到原投影。固定高度/距离限幅、TF、雷达驱动与下位机未改；新增节点及依赖已编译安装。实测 `/scan` 发布为 BEST_EFFORT，而 RViz 配置为 RELIABLE，已修正两份 RViz 扫描订阅并移除无效 QoS 覆盖，便于准确对照扫描。
- 离线 120 帧全部通过平面验证，前方候选 3537→0，总候选 496302→476075，左侧离地点 425→425；拟合中位约 3.5ms/帧、95% 约 4.8ms。左侧后续 120 帧没有同区域离地原始点，但历史代价地图仍有残留；按用户最新要求停止对此项继续诊断，不屏蔽左侧、不修改代价地图清除策略。
- 人物综合阈值从 0.10 改为 0.15；ReID 90%/Color 10%、分差 0.08、连续三次确认和猫阈值 0.75 保留。同步 README、网页导航说明与模型说明，边界测试改为 0.149 拒绝/0.15 接受。
- 验证：5 项地面/障碍保留/失败透传/原始消息字段测试，以及域 98 中真实过滤节点接真实 pointcloud_to_laserscan 的 1 项测试通过；人物/会话 11 项和网页/视频 3 项测试通过。合成测试包含 5cm 低障碍、细杆、无地面、小片平面、倾角变化及原图消息时间戳保留。colcon build、launch --show-args 与差异检查通过。
- 限制：3cm 过滤带会舍弃同高度小凸起，原有近车低障碍盲区仍存在；运动振动、坡面效果尚未实车验证。未自动重启正在运行的导航或清除地图；需停止追踪后重启完整导航加载过滤节点，重启网页使人物阈值生效。相机分辨率保持 640×480，本次尚未提交。

### 20260922-07：提高自动追踪平移速度上限

- 用户反馈自动追踪移动缓慢。只读确认运行中 FollowTarget.max_speed_xy=0.20、AlignTarget.max_speed_xy=0.20/max_vel_theta=0.4，网页在启用及持续追踪时也发送固定 0.20 m/s SpeedLimit，形成两处限速。
- 自动追踪平移上限统一提高为 0.50 m/s：FollowNavigation 使用 FOLLOW_SPEED_LIMIT 常量，FollowTarget x/y 范围为 ±0.50，合速度最多 0.50。前后、横移、斜移共用此上限，不改变普通导航或键盘速度参数。
- AlignTarget 的 x/y 仍为 0、角速度仍限 0.4 rad/s；仅把用于 SpeedLimit 比例换算的 max_speed_xy 基准同步为 0.50，防止提升平移限速时意外放大纠偏转速。1 米停车目标、1.1/1.3 米滞回、目标丢失暂停、碰撞检查和手动停止逻辑保留。
- 串口平移倍率 3.5 与转向倍率 10 保留，未修改下位机。0.50 m/s 是指令上限，实际车速变化仍受固件/机械影响；接近目标时每轴小于约 0.143 m/s 的量化归零问题仍存在，不宣称已解决末端低速控制。
- 验证：69 项网页/策略/浏览器/隔离 ROS 回归通过；隔离域 97 的真实 Nav2 5 项测试通过，新增确认直行/横移/斜移能输出超过 0.30 m/s、合速度不超过 0.50，并在实际 0.50 SpeedLimit 下保持纠偏不超过 0.4 rad/s，碰撞阻挡及普通导航朝向检查通过。两仓库 git diff --check、安装参数链接检查通过。
- 页面提示及两层使用说明已同步。安装 YAML 为源码链接，无需重新编译；需停止追踪后重启完整导航与网页并刷新。未重启当前服务、未向真实车辆发送测试运动；提速后的实车速度和停车距离尚未验证，本次尚未提交。

### 20260922-08：整理工作日志并提交本轮累计修改

- 按用户要求将本轮累计实现与日志一并提交：网页新增人物测试模式和专用 OmniScaleNet ReID，当前人物综合阈值为 0.15（ReID 90%、Color 10%），猫阈值为 0.75；识别模式切换及迟到推理结果隔离、评分显示和相关测试纳入版本控制。
- 自动追踪持续等待目标重获，短暂丢失后暂停导航；平移阶段使用麦轮专用控制器，车体偏角达到 20° 后纠偏、回到 8° 内恢复平移，使云台 Yaw 尽量回中。Yaw 指令变化速度 120°/s，平移上限 0.50 m/s、纠偏角速度上限 0.4 rad/s、停车目标约 1 米。
- 工作空间提交包含导航加减速/末端姿态参数、速度队列只保留最新指令、专用导航行为树、前方地面过滤与 RViz 扫描 QoS 修正、只读速度诊断脚本及隔离测试。保留底盘标定倍率；本轮不处理左侧假点，不修改 Cat_C，下位机仓库保持干净。
- 相机分辨率调整已按用户要求撤销，维持 640×480；不提交撤销掉的高清模块。上传照片、Python 缓存及本机模型缓存均排除，保留原文件，不为清理工作区而删除用户数据。
- 提交前检查沿用本轮已通过的 69 项网页完整测试、5 项真实 Nav2 隔离测试、6 项地面过滤/真实转换链路测试；此前速度响应与伪串口验证记录见 20260921-05/20260922-01。两仓库 git diff --check 通过。未通过实车运动验证跟随速度、停车距离或运动中的地面过滤效果。
- 提交顺序为网页仓库先提交代码、说明、测试与本日志，然后工作空间提交导航/雷达/驱动改动并更新网页 gitlink。仅创建本地 Git 提交；完整导航和网页服务需重启后加载变更，未自动重启当前服务。
